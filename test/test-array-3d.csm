// 3d array
Array<Array<Array<int>>> c = 
    [
        [[1], [2]], 
        [[3], [4]], 
        [[5], [6]]
    ];

for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 2; j++) {
        c[i][j][0] *= 2;
        println(c[i][j][0]);
    }
}

delete c;
