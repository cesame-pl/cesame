struct Student {
    String name;
    int age;
    //Array<String> courses;
};

// Student a = new Student;
Student a;
a.name = "Mike";
a.age = 22;
//a.courses = ["math", "history", "cs"];
//a.courses[0] = "french";

Student b;
b.name = "Alice";
b.age = 24;
//b.courses = ["plt", "os", "database"];

/* Not yet supported */
// c = new Student;
// Student c =  { .name = "Alice", .age = 24, .courses = ["PLT", "OS"] };

/* Not yet supported */
// Student d;
// d = { .name = "Bob", .age = 52 };

Array<Student> students = [a, b];
for (int i = 0; i < 2; i++) {
    for (int j = 0; j < 3; j++) {
        String s = students[1].name;
        print(s);
    }
}

