// 2d array

Array<Array<int>> b = [[-1, -2], [-3, -4], [-5, -6]];
for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 2; j++) {
        b[i][j] = i * 2 + j;
        println(b[i][j]);
    }
}

delete b;